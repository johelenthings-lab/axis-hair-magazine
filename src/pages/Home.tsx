import React from 'react';
import Archive from './Archive';

const Home: React.FC = () => {
  // In the refined editorial model, the Home page acts as the entry to the Archive collection.
  return <Archive />;
};

export default Home;
